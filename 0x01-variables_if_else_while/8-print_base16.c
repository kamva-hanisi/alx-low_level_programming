#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
	char n;

	int d;
	
	c = 'a';
	d = 0;

	while
	       	(d < 10) {
			putchar(d + '0');
			d++;
		}
	while
		(c <= 'f') {
			putchar(c);
		}
	putchar('\n');
	return (0);
}
